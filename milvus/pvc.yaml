---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: milvus-data
  namespace: milvus
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # Uses cluster default StorageClass (e.g. local-path on NVMe-backed nodes).
  # storageClassName: local-path
